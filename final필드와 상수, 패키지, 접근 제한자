final필드란 최종적인 값을 갖고 있는 필드로 값을 변경할 수 없는 필드이다. 객체마다 가지는 불변의 인스턴스 필드이다.
어떤 방식이든 값이 설정되면 바꿀 수 없다.
final필드의 초기값 지정 방법은 변수를 만듦과 동시에 초기화 하는 방법과 생성자를 통해서 주는 방법 총 두가지가 있다.

상수란 정적인 final필드라고 보면 된다. 상수는 객체마다 가지고 있지 않으며 메소드 영역에서 클래스 별로 관리되는 불변의 정적 필드이다.
수학의 원주율이나 지구의 반지름이나 지구의 무게 등 공용 데이터를 다룬다.
상수 이름은 전부 대문자로 작성해야 한다.
다른 단어가 결합되면 _(언더바)로 연결된다. (스네이프 표기법)

패키지는 클래스를 기능별로 묶어서 이름을 붙여 놓은 것이다. 제일 위에 선언되며 물리적인 형태는 파일 시스템의 폴더와 같다.
패키지는 단순한 폴더로 기능하는게 아니라 클래스의 일부분으로 인식한다. 
패키지는 클래스를 컴파일하는 과정에서 자동적으로 생성되는 폴더이다.
전체 클래스 이름= 상위패키지.하위패키지.클래스
클래스명이 같아도 패키지명이 다르면 다른 클래스로 취급된다. 
기업에서는 회사 도메인을 주로 패키지명으로 쓴다. 예시) com.naver.ClassA / com.google.ClassB/ com.nate.Car

import문은 패키지 내에 같이 포함된 클래스간 클래스 이름으로 사용 가능하다.
